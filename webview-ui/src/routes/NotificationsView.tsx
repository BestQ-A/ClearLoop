/**
 * Notifications 全屏页（NotificationPopover 的非 popover 形式）。
 * 占位骨架，实际实现由 NAV agent 填充。
 */
export default function NotificationsView() {
  return (
    <div className="p-4 text-sm text-[var(--vscode-descriptionForeground)]">
      NotificationsView placeholder — assigned to NAV agent
    </div>
  );
}
