/**
 * Kanban View —— Phase 列表（注意：Traycer 实际是垂直三段折叠列表，非 Kanban 列）。
 * 占位骨架，实际实现由 EPIC agent 填充。
 */
export default function KanbanView() {
  return (
    <div className="p-4 text-sm text-[var(--vscode-descriptionForeground)]">
      KanbanView placeholder — assigned to EPIC agent
    </div>
  );
}
