/**
 * Task View —— phase breakdown / task detail 页面。
 * 占位骨架，实际实现由 EDITOR + EPIC agent 后续填充。
 */
export default function TaskView() {
  return (
    <div className="p-4 text-sm text-[var(--vscode-descriptionForeground)]">
      TaskView placeholder — assigned to EDITOR agent
    </div>
  );
}
