/**
 * Remote MCP Server 配置页。
 * 占位骨架，实际实现由 SETTINGS agent 填充。
 */
export default function McpView() {
  return (
    <div className="p-4 text-sm text-[var(--vscode-descriptionForeground)]">
      McpView placeholder — assigned to SETTINGS agent
    </div>
  );
}
