/**
 * Epic Chat View —— Epic 详情页（左主区 + 右 Artifacts 抽屉）。
 * 完整布局参见 TRAYCER_EPIC_UI_TEARDOWN.md §A，使用 ResizablePanelGroup。
 * 占位骨架，实际实现由 EPIC agent 填充。
 */
export default function EpicChatView() {
  return (
    <div className="p-4 text-sm text-[var(--vscode-descriptionForeground)]">
      EpicChatView placeholder — assigned to EPIC agent
    </div>
  );
}
